{
  "name": "@responsible-vibe/cli",
  "version": "3.1.6",
  "description": "CLI tools for responsible-vibe development workflows",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean:build": "rimraf ./dist",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@responsible-vibe/core": "workspace:*",
    "@responsible-vibe/mcp-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
